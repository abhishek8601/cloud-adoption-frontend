# Cloud Adoption Conference App

Expo Router app for the Cloud Adoption Summit sign-in experience.

## Run locally

```bash
npm install
npx expo start
```

## Main screen

The current sign-in screen lives in `src/app/index.tsx`.
